source 'https://rubygems.org'

gem 'rubyzip', '~> 3.0'  # For handling ZIP files
gem 'sqlite3', '~> 2.0'  # For database
gem 'dotenv', '~> 3.0'  # For environment variables
gem 'logger', '~> 1.5'  # For logging
gem 'fileutils', '~> 1.7'  # For file operations
gem 'json', '~> 2.6'  # For JSON handling
gem 'net-http', '~> 0.2'  # For HTTP requests

group :test do
  gem 'rspec', '~> 3.13'
end